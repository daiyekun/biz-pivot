# ============ Web 框架 ============
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0

# ============ 数据库 / ORM ============
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9.10

# ============ 缓存 / 消息队列 / 向量库 ============
redis>=5.2
celery>=5.4,<6.0
qdrant-client>=1.12

# ============ 认证 / 安全 ============
# 密码加密采用 stdlib hashlib.pbkdf2_hmac（pwd + slot 加盐），无需额外依赖
PyJWT>=2.10

# ============ HTTP / LLM / 文件 ============
httpx>=0.28
python-multipart>=0.0.12
tiktoken>=0.8

# ============ 可观测性 (OpenTelemetry -> Jaeger) ============
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-grpc>=1.27